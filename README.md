# AGE-CALCULATOR
Users enter their name and date of birth, and the backend processes the input to calculate the precise age relative to the current system date. The result is displayed instantly on the page. Each calculation is also stored in a CSV file, allowing users to view their previous age checks through a history modal.
